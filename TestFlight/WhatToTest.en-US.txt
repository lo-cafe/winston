- Many performance improvements (one step at a time), including a new markdown renderer and the removing of a useless library.
- Fixes posts not opening in search
- Removed tooltip in votes, since it was causing a bunch of lag
